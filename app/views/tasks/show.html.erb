<div class="container">
  <h2><%= @task.title %></h2>
  <p><%= @task.details %></p>
  <%= check_box("task", @task.id, {checked: @task.completed}) %>
  <br>
  <%= link_to 'Go back !', tasks_path %>
</div>
